name: Build and share Docker Image to Docker Hub

on:
  push:
    tags:
      - '*'  # Triggers on any tag push
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Docker image tag (e.g., latest, 1.0.1)'
        required: true
        default: 'latest'

jobs:
  build-and-share:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: docker/Dockerfile 
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_REPO_NAME }}:${{ github.event.inputs.tag_name }}

      - name: Build and push celery image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: docker/CeleryDockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_CELERY_REPO }}:${{ github.event.inputs.tag_name }}